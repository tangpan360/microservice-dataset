# 微服务负载预测任务定义

## 1. 研究目标

本文希望定义一个能够同时适用于多个微服务数据集的统一预测任务，使不同来源、不同规模、不同采集方式的数据都服务于同一个问题，而不是分别为不同任务构造不同标签。

结合前期对综述论文、阿里 2022 微服务数据集以及自建 `online_boutique_clarknet` 数据集的分析，本文将微服务负载预测的核心问题定义为：

**面向动态依赖关系的服务级需求传播预测（service-level demand propagation forecasting）。**

更具体地说，本文不把“CPU 利用率预测”或“实例数预测”作为统一主任务，而是把预测目标放在更接近系统真实需求、且更少受资源管理策略直接干扰的量上。

---

## 2. 为什么不直接预测 CPU、内存或实例数

不同数据集中的 CPU、内存、实例数等状态量，其生成机制并不一致。

- 在 `clarknet` 数据集中，采集时没有自动扩缩容，实例数基本固定，因此 CPU、内存的变化更接近“固定部署下对负载的响应”。
- 在阿里生产数据中，资源状态、实例活跃情况、性能表现可能受到真实部署、资源调度、弹性机制和线上控制策略的共同影响。
- 如果未来再引入其他数据集，不同控制策略、采样方式、资源限制也会进一步改变这些状态量的统计规律。

因此，若直接把 CPU、内存或实例数作为所有数据集统一的主预测目标，则会出现标签语义不统一的问题：不同数据集实际上预测的不是同一个对象。

相比之下，服务被调用的强度、调用在服务图中的传播，以及由此形成的服务级需求，更接近跨数据集共享的本质问题。

---

## 3. 核心概念

### 3.1 服务级入负载（incoming demand）

服务级入负载表示某个服务在一个时间窗口内接收到的调用需求总量。

对服务 `s`，在时间窗口 `t` 中，其入负载定义为：

- 所有上游服务对 `s` 的调用次数之和；
- 若 `s` 为入口服务，则其入负载还可以包含来自系统外部的入口请求。

直观理解：

**入负载 = 这个服务在该时间窗口内“被打了多少次”。**

### 3.2 边级调用强度（edge call intensity）

边级调用强度表示某条服务调用边在一个时间窗口内承载了多少调用。

对边 `(u, v)`，在时间窗口 `t` 中，其调用强度定义为：

- 服务 `u` 对服务 `v` 发起的调用次数。

直观理解：

**调用强度 = 某个上游服务对某个下游服务“打了多少次”。**

### 3.3 服务级压力/性能响应（pressure / performance response）

服务级压力或性能响应表示服务在给定需求条件下表现出的资源和服务质量状态，例如：

- 平均延迟
- P95 延迟
- 错误率
- 资源使用强度

这类量很重要，但更适合作为辅助任务或第二阶段任务，而非所有数据集统一的唯一主任务。

---

## 4. 统一任务定义

### 4.1 主任务

**未来多步的服务级入负载预测**

给定过去 `L` 个时间步内的服务级观测和动态调用关系，预测未来 `H` 个时间步内每个服务的入负载。

形式化地，对每个服务 `s`，预测：

\[
Y^{node}_{s,t} = [d_{s,t+1}, d_{s,t+2}, ..., d_{s,t+H}]
\]

其中 `d_{s,t+h}` 表示服务 `s` 在未来第 `t+h` 个时间步的入负载。

### 4.2 联合任务

**未来多步的边级调用强度预测**

对每条服务边 `(u,v)`，预测未来 `H` 个时间步内该边的调用强度：

\[
Y^{edge}_{u,v,t} = [c_{u,v,t+1}, c_{u,v,t+2}, ..., c_{u,v,t+H}]
\]

其中 `c_{u,v,t+h}` 表示未来该边的调用次数。

### 4.3 辅助任务

**未来多步的服务级压力/性能预测**

例如：

- `latency_mean`
- `latency_p95`
- `error_rate`
- 自定义压力分数

这类目标用于刻画需求传播后的系统响应，而不是替代主任务。

---

## 5. 为什么选择这个任务

该任务具有以下优点：

1. **跨数据集语义更统一**  
   不同数据集在资源状态量上可能受策略影响不同，但服务被调用的需求传播逻辑更接近系统本质。

2. **更贴合微服务特征**  
   微服务系统的核心不是单个时间序列，而是调用关系驱动的传播与耦合。

3. **更利于后续资源管理应用**  
   入负载和调用强度可以直接服务于扩缩容、热点识别、容量规划和资源预分配。

4. **避免把控制策略的结果误当作负载规律**  
   先预测需求，再讨论资源和性能响应，比直接预测 CPU 或副本数更合理。

---

## 6. 数据集上的统一映射

## 6.1 Clarknet Online Boutique

可直接使用整理后的分钟级表：

- 节点表：`service_minute_features`
- 边表：`service_call_edge_minute`

### 主标签

- `service_in_rps`

### 联合标签

- `call_count`

### 辅助标签

- `service_latency_mean_ms`
- `service_latency_p95_ms`
- `service_latency_p99_ms`
- `service_error_rate`

### 当前推荐的关键输入特征

在当前阶段，若主任务先收敛为**服务级未来入负载预测**，则不建议一开始把所有可用列都放入模型，而应优先使用跨数据集更容易统一、且与需求传播直接相关的特征。

#### 节点级核心特征

- 服务自身历史流量：`service_in_rps`, `service_out_rps`
- 时间周期特征：`hour_sin`, `hour_cos`, `dow_sin`, `dow_cos`, `is_weekend`
- 服务身份特征：`service_idx`

#### 节点级上下游上下文特征

- `upstream_total_calls`
- `upstream_service_count`
- `downstream_total_calls`
- `downstream_service_count`
- `top1_upstream_calls`, `top2_upstream_calls`, `top3_upstream_calls`
- `top1_downstream_calls`, `top2_downstream_calls`, `top3_downstream_calls`

#### 边级特征

- `call_count`

#### 可选增强特征

以下特征在 `Clarknet` 上可用，但不建议作为第一版核心依赖，而更适合作为后续增强实验：

- 服务性能特征：`service_latency_mean_ms`, `service_latency_p95_ms`, `service_latency_p99_ms`
- 服务资源特征：`cpu_cores`, `memory_working_set_bytes`
- 边性能特征：`latency_mean_ms`, `latency_p95_ms`, `latency_max_ms`

#### 当前不建议作为统一核心特征

以下特征更偏部署状态、资源控制状态或数据集特有语义，不建议作为当前跨数据集统一主方案的核心输入：

- `replica_count`, `ready_replica_count`, `active_node_count`
- `cpu_throttling_ratio`
- `service_error_rate`, `service_timeout_rate`
- 原始时间整数列：`hour_of_day`, `day_of_week`, `minute_of_day`

### 任务边界

由于该数据集采集时没有自动扩缩容，因此它更适合研究：

- 固定部署条件下的需求传播
- 固定部署条件下的性能与资源响应

而不适合作为“不同自动扩缩容策略下状态预测”的直接证据。

## 6.2 阿里 2022 微服务数据集

阿里数据需要先进行统一聚合，构造成与 `Clarknet` 尽量一致的服务级表结构。

当前第一版主干提取只强依赖：

- `CallGraph`

后续完整数据集可逐步扩展：

- `MSMetrics`
- `MSRTMCR`

当前推荐的主标签与关键输入如下。

### 主标签

- 每分钟每个服务被调用的总次数，即 `service_in_calls_per_min`

### 联合标签

- 每分钟每条边的调用次数，即 `call_count`

### 当前推荐的关键输入特征

#### 节点级核心特征

- `service_in_calls_per_min`
- `service_out_calls_per_min`
- `external_inflow_calls`
- `hour_sin`, `hour_cos`
- `service_idx`

#### 节点级上下文特征

- `upstream_total_calls`
- `upstream_service_count`
- `downstream_total_calls`
- `downstream_service_count`
- `top1_upstream_calls`, `top2_upstream_calls`, `top3_upstream_calls`
- `top1_downstream_calls`, `top2_downstream_calls`, `top3_downstream_calls`

#### 边级特征

- `caller_service`
- `callee_service`
- `call_count`

#### 可选增强特征

- `edge_rt_mean`, `edge_rt_p95`, `edge_rt_max`
- 基于 `MSMetrics` 聚合的服务级 CPU / memory 特征
- 基于 `MSRTMCR` 聚合的更细粒度交互统计

### 当前推荐原则

- 第一版先使用 `CallGraph` 主干表完成主任务；
- 服务筛选主要作用于监督目标，而不是直接删除图中的桥接节点；
- 阿里数据在建模时更适合作为大规模、局部子图条件下的真实复杂场景验证。

### 提取处理文档

`Ali2022` 的具体提取规则、脚本职责、筛选逻辑、`top200` 探索记录、局部子图候选构造规则，以及后续完整数据集扩展计划，已单独迁移到：

- `Ali2022数据提取文档.md`

### 任务边界

阿里数据是大规模真实系统数据，服务多、边多、动态强，因此更适合验证：

- 方法在真实复杂场景下是否有效
- 方法能否适应大图和长尾服务
- 方法是否具有规模泛化能力

---

## 7. 图建模方式

由于不同数据集的图规模差异极大，不能采用完全相同的全图建模方式。

### 7.1 Clarknet：全图建模

Clarknet 仅包含 11 个服务，图规模小、结构相对清晰，适合直接做全图动态图建模。

### 7.2 阿里：局部子图建模

阿里数据服务数量超过两万，若直接做全图图时序建模，计算成本和稀疏性问题都会非常严重。

因此更合理的方式是：

**对每个目标服务，仅提取其局部依赖子图（如 1-hop/2-hop 或 top-k 邻居）进行建模。**

这样可以统一小图和大图：

- 在 Clarknet 上，局部子图几乎等于整图
- 在阿里上，局部子图是目标服务的关键上下游上下文

### 7.3 局部子图中的特征语义约束

局部子图的主要作用是为目标服务提供**结构上下文**，而不是重新定义目标服务的真实总流量统计。因此，需要明确区分两类特征：

#### 全量真实特征

以下特征应尽量来自全量服务级聚合表，而不是从截断后的局部子图中重新求和：

- 目标服务的 `service_in_calls_per_min`
- 目标服务的 `service_out_calls_per_min`
- 目标服务的 `external_inflow_calls`
- 全量 `upstream_total_calls`
- 全量 `upstream_service_count`
- 全量 `downstream_total_calls`
- 全量 `downstream_service_count`

这类量应被视为**真实节点级统计**，可直接作为标签或目标节点自身输入特征。

#### 局部截断特征

若某些统计量是基于局部子图中保留下来的节点和边重新计算得到的，则它们不再等于全量真实值，而应被视为**局部上下文特征**，例如：

- 局部图中保留边的入流量和
- 局部图中保留边的出流量和
- 局部图中保留的邻居数
- 局部图中的局部边密度

这类特征建议显式命名为 `local_*`，以避免和全量真实统计混淆。

#### 当前推荐原则

- 标签与目标服务自身核心统计，优先使用全量服务级聚合结果；
- 局部子图主要用于提供邻域结构、强连接边和上下游上下文；
- 不应使用“截断子图重新求和”的结果去替代目标服务的全量真实总流量；
- 论文表述中应明确：局部子图用于建模上下文，而不是用于近似恢复完整系统调用图。

---

## 8. 一个通用模型还是每个微服务一个模型

本文不建议为每个微服务训练一个完全独立的模型，也不建议完全忽略服务差异使用一个对所有服务“一视同仁”的黑盒模型。

更合理的方案是：

**共享参数的全局模型 + 服务感知表示 + 局部子图条件化建模**

即：

- 用一个共享主模型学习通用规律
- 用服务身份、服务角色、静态画像或局部图上下文表达异质性
- 输入目标服务及其局部依赖子图
- 输出目标服务未来的需求与压力

这样既能保证：

- 跨服务共享知识
- 适应新服务和长尾服务
- 保留微服务之间的异质性

又避免：

- per-service model 的不可扩展性
- 完全统一模型的过度平滑

---

## 9. 建议的方法框架

推荐采用：

**共享参数、服务感知、局部子图的节点-边联合动态图预测框架**

### 输入

过去 `L` 个时间步的：

- 目标服务自身特征序列
- 局部邻居服务特征序列
- 局部边特征序列
- 时间特征
- 服务角色或静态画像

### 输出

- 未来 `H` 个时间步的服务级入负载
- 可选未来 `H` 个时间步的边级调用强度
- 可选未来 `H` 个时间步的压力/性能指标

### 推荐设置

- 历史窗口 `L`：30 或 60 分钟
- 预测步长 `H`：5 / 15 / 30 分钟

---

## 10. 可对比的现成模型

### 10.1 基础时间序列基线

- Last Value
- Moving Average
- ARIMA / SARIMA
- XGBoost / LightGBM with lag features
- LSTM / GRU
- TCN
- DLinear / NLinear
- PatchTST

### 10.2 图时序基线

- STGCN
- DCRNN
- Graph WaveNet
- MTGNN
- AGCRN

### 10.3 微服务领域相关模型

- KAE-Informer
- 已阅读的微服务图模型论文（如 2025 年的图对比学习或时空图模型）

---

## 11. 创新点

本文方案的创新点不应仅仅停留在“用了图模型”，而应集中在以下几个方面。

### 11.1 任务定义创新

将微服务负载预测从策略敏感的资源状态预测，重构为：

**动态服务图中的服务级需求传播预测。**

这样能更自然地统一固定部署数据和真实生产数据。

### 11.2 跨规模统一建模

通过“目标服务中心的局部子图”统一小图与大图：

- 小图可看作局部子图接近整图
- 大图只需围绕目标服务提取关键上下游

### 11.3 共享与异质性的平衡

采用共享模型 + 服务感知表示，兼顾：

- 通用规律学习
- 服务异质性表达
- 大规模可扩展性

### 11.4 节点-边联合预测

同时预测：

- 服务未来被调用负载
- 服务间未来调用强度

从而更好地刻画微服务系统中的传播机制。

---

## 12. 需要注意的风险

1. **不要把 CPU/内存当作所有数据集统一主标签。**  
   这类状态量容易混入控制策略、部署方式和资源管理反馈。

2. **不要只在 Clarknet 小图上证明方法有效。**  
   小图全图建模的成功不能自动推出大规模场景也成立。

3. **不要把 per-service model 作为主线方案。**  
   它不具备大规模可扩展性，也不符合通用任务目标。

4. **不要只做单节点回归。**  
   若忽略边的传播作用，则会削弱微服务问题本身的结构特征。

---

## 13. 当前推荐的最终任务表述

本文研究的问题可以概括为：

> 给定过去一段时间内微服务系统的服务级状态和动态调用关系，如何预测未来多个时间步内每个服务的被调用需求以及服务间调用传播强度，并进一步刻画需求传播对系统压力和性能的影响。

对应地：

- **主任务**：服务级未来入负载预测
- **联合任务**：边级未来调用强度预测
- **辅助任务**：服务级未来压力/性能预测

这是当前最适合作为多个数据集统一目标的任务定义。
