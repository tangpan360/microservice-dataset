# Ali2022 数据提取文档

## 1. 文档目的

本文档专门记录 `Ali2022` 微服务数据当前的提取、清洗、筛选和局部子图构造流程，用于回答以下问题：

- 当前到底从阿里原始数据里提取了哪些内容；
- 这些内容是如何一步步提取出来的；
- 当前哪些只是第一版主干特征；
- 后续若要扩展成完整数据集，还需要补提哪些部分。

这份文档与 `任务定义.md` 分工不同：

- `任务定义.md` 负责说明研究问题、任务映射和模型主线；
- 本文档负责说明 `Ali2022` 的具体数据处理实现与当前进展。

---

## 2. 当前处理目标

当前第一阶段的目标不是一次性把阿里所有原始文件都提成最终完整版数据集，而是优先构造一套与 `Clarknet` 尽量对齐、能够支撑主任务的分钟级主干表。

当前主任务聚焦于：

- 服务级未来入负载预测；
- 可选的边级未来调用强度预测。

因此，当前第一版提取优先依赖：

- `CallGraph`

后续计划继续接入：

- `MSMetrics`
- `MSRTMCR`

当前不作为主线必需项：

- `NodeMetrics`

---

## 3. 原始数据来源与当前优先级

### 3.1 原始主要数据来源

- `CallGraph`
- `MSMetrics`
- `MSRTMCR`

### 3.2 当前第一版最小可行输入

若当前目标限定为服务级未来入负载预测，则第一版最小可行文件集为：

- `CallGraph`：构造服务级入/出调用、边级调用次数、上下游聚合统计和基础时间特征。

### 3.3 后续扩展方向

更完整但仍然稳妥的扩展方式：

- 在 `CallGraph` 主干表基础上，接入 `MSMetrics`，补充服务级 CPU / memory 特征；
- 再按需要接入 `MSRTMCR`，补充更细粒度的交互时延或交互频率特征。

---

## 4. 当前实现脚本与职责

`preprocess_ali/` 下当前已有 3 个脚本：

### 4.1 `build_ali2022_intermediate.py`

职责：

- 从原始 `CallGraph` 批量提取分钟级中间表；
- 生成服务级分钟表、边级分钟表、服务元数据表、运行元数据表；
- 支持并行处理；
- 支持进度条输出；
- 支持只处理前 `N` 个文件，便于探索性分析。

当前关键参数包括：

- `--trace-root`
- `--output-root`
- `--run-id`
- `--scenario-id`
- `--app-name`
- `--max-files`
- `--workers`
- `--clean-output`

默认输出根目录为：

- `dataset/processed/ali2022_callgraph`

### 4.2 `filter_services_from_metadata.py`

职责：

- 基于 `service_metadata.csv` 做第一轮目标服务粗筛；
- 计算质量分数；
- 输出目标服务列表与筛选摘要。

关键输入输出：

- 输入：`service_metadata.csv`
- 输出：`target_services.csv`
- 输出：`target_services_summary.json`

### 4.3 `build_target_subgraph_candidates.py`

职责：

- 基于筛选出的目标服务和边级分钟表，构造目标服务中心的局部子图候选；
- 支持 `1-hop/2-hop`；
- 支持 `top-k` 上下游邻居截取；
- 支持 `max_nodes` 限制。

关键输入输出：

- 输入：`target_services.csv`
- 输入：`service_call_edge_minute/`
- 输出：局部子图候选目录及摘要文件

---

## 5. 当前实际提取流程

当前实际处理流程分为三步。

### 5.1 第一步：从 `CallGraph` 提取分钟级主干表

脚本：

- `preprocess_ali/build_ali2022_intermediate.py`

目标：

- 先只依赖 `CallGraph`，构造与 `Clarknet` 尽量对应的主干分钟级表；
- 优先保证语义稳定，而不是一开始就提取所有增强特征。

当前优先提取的主干字段：

- `service_in_calls_per_min`
- `service_out_calls_per_min`
- `external_inflow_calls`
- `upstream_total_calls`
- `upstream_service_count`
- `downstream_total_calls`
- `downstream_service_count`
- `top1_upstream_calls`
- `top2_upstream_calls`
- `top3_upstream_calls`
- `top1_downstream_calls`
- `top2_downstream_calls`
- `top3_downstream_calls`
- `caller_service`
- `callee_service`
- `call_count`
- 可选 `edge_rt_mean`
- 可选 `edge_rt_p95`
- 可选 `edge_rt_max`

### 5.2 第二步：基于 `service_metadata` 做服务筛选

脚本：

- `preprocess_ali/filter_services_from_metadata.py`

目标：

- 不先凭经验选服务；
- 先观察真实活跃度和流量分布，再筛目标服务集合；
- 筛选主要作用于监督目标，而不是先验删除图中的桥接服务。

在筛选阶段区分以下集合：

- `S_all`：全量观测服务集合
- `S_candidate`：候选建模服务集合
- `S_train`：最终训练/评估服务集合
- `S_context`：图上下文服务集合

需要明确：

- 服务筛选不等于图节点删除；
- 即使某些服务不作为监督目标，也可能仍应保留为上下文节点。

### 5.3 第三步：围绕目标服务构造局部子图候选

脚本：

- `preprocess_ali/build_target_subgraph_candidates.py`

目标：

- 从全量边结构中围绕目标服务提取局部上下文；
- 用于后续局部子图建模、目标服务比较和候选数据集构造。

当前支持参数包括：

- `--max-hop`
- `--max-nodes`
- `--topk-in`
- `--topk-out`
- `--topk-next-hop`

---

## 6. `CallGraph` 的当前具体提取规则

### 6.1 时间处理

`CallGraph` 的原始 `timestamp` 不是天然按分钟对齐，因此先做分钟落桶。

当前实现对应：

- `minute_bucket_s = floor(timestamp_ms / 60000) * 60`

同一服务和边的所有统计量都以该分钟桶为统一时间键。

### 6.2 服务名映射

原始 `CallGraph` 中：

- `um` 视为 `caller_service`
- `dm` 视为 `callee_service`

为与 `Clarknet` 对齐，中间结果统一使用：

- `caller_service`
- `callee_service`

### 6.3 行解析策略

当前实现使用更稳妥的按尾部索引读取方式，而不是依赖不稳定列位：

- `caller_service = row[-6]`
- `callee_service = row[-3]`
- `rt = row[-1]`

并使用 `safe_float()` 兼容：

- 空字符串
- `"None"`
- 无法正常解析的异常值

### 6.4 `UNKNOWN` 的处理

当前规则：

- `callee_service = UNKNOWN`：不纳入显式服务节点和普通边表；
- `caller_service = UNKNOWN` 且 `callee_service != UNKNOWN`：不作为普通服务边，但保留为目标服务的 `external_inflow_calls`；
- `caller_service = callee_service` 的自调用：当前第一版保留，不直接删除。

### 6.5 去重策略

当前第一版不对 `CallGraph` 行做额外去重。

原因：

- 相同 `(timestamp, um, dm)` 的多行记录不一定是脏数据；
- 它们也可能对应真实调用事件或不同层级的调用片段。

---

## 7. 当前中间表输出结构

### 7.1 `service_call_edge_minute`

从 `CallGraph` 经分钟落桶后，按以下键聚合：

- `timestamp_bucket_s`
- `caller_service`
- `callee_service`

当前建议输出字段：

- `timestamp_bucket_s`
- `caller_service`
- `callee_service`
- `call_count`
- `edge_rt_mean`
- `edge_rt_p95`
- `edge_rt_max`
- `caller_idx`
- `callee_idx`
- `run_id`
- `scenario_id`
- `day`

字段含义：

- `call_count = count(*)`
- `edge_rt_mean = mean(rt)`
- `edge_rt_p95 = p95(rt)`
- `edge_rt_max = max(rt)`

### 7.2 `service_metadata`

从服务级统计信息构造，当前核心字段包括：

- `service`
- `service_idx`
- `first_seen_timestamp_bucket_s`
- `last_seen_timestamp_bucket_s`
- `total_active_minutes`
- `total_in_calls`
- `total_out_calls`
- `nonzero_in_minutes`
- `avg_in_calls`
- `max_in_calls`

其中：

- `service_idx` 按服务名排序后稳定编号；
- `first_seen_timestamp_bucket_s` / `last_seen_timestamp_bucket_s` 由服务在 `CallGraph` 中首次/最后一次出现的分钟桶得到；
- `avg_in_calls` 由累计入调用量和活跃分钟数计算；
- `nonzero_in_minutes` 用于后续筛选稀疏服务。

### 7.3 `service_minute_features`

优先基于边级分钟表二次聚合得到，当前主干字段包括：

- `timestamp_bucket_s`
- `service`
- `service_in_calls_per_min`
- `service_out_calls_per_min`
- `external_inflow_calls`
- `upstream_total_calls`
- `upstream_service_count`
- `downstream_total_calls`
- `downstream_service_count`
- `top1_upstream_calls`
- `top2_upstream_calls`
- `top3_upstream_calls`
- `top1_downstream_calls`
- `top2_downstream_calls`
- `top3_downstream_calls`
- `day_index`
- `hour_of_day`
- `minute_of_day`
- `hour_sin`
- `hour_cos`
- `service_idx`
- `run_id`
- `scenario_id`
- `day`

字段说明：

- `service_in_calls_per_min`：对所有流入该服务的边做 `call_count` 求和，并与 `external_inflow_calls` 合并；
- `service_out_calls_per_min`：对所有从该服务流出的边做 `call_count` 求和；
- `upstream_service_count`：统计流入该服务的不同上游服务数；
- `downstream_service_count`：统计该服务流出的不同下游服务数；
- `topk_upstream_calls` / `topk_downstream_calls`：对入边/出边调用次数排序后取前 `k`，不足补 `0`。

### 7.4 `run_metadata`

当前用于记录单次提取运行的基础元信息，字段包括：

- `run_id`
- `scenario_id`
- `app_name`
- `start_time_s`
- `end_time_s`
- `bucket_seconds`
- `service_count`
- `day`
- `day_index`

---

## 8. 当前并行实现方式

`build_ali2022_intermediate.py` 当前已改造成并行版本，核心流程为两阶段：

### 8.1 第一阶段：并行收集服务集合

- 将 `CallGraph` 文件分块；
- 每个 worker 扫描其负责文件块中的服务名；
- 主进程合并全局服务集合；
- 统一生成稳定的 `service_idx` 映射。

### 8.2 第二阶段：并行处理文件块

- 每个 worker 处理一个文件块；
- 在 worker 内聚合分钟级边与服务统计；
- 按天写出各自的 `part-xxxxx.csv`；
- 主进程再合并 `ServiceStats` 与按天摘要。

### 8.3 当前实现特点

- 支持 `ProcessPoolExecutor` 并行；
- 支持 chunk 粒度的进度条；
- 允许 `--max-files` 做前缀子集探索；
- 支持 `--clean-output` 清理旧运行目录。

当前需要注意：

- 高并行下容易转为 I/O 受限，而不一定持续高 CPU；
- 进度条以 chunk 完成为单位更新，因此在大 chunk 内可能显得“长时间不动”；
- 当前输出采用 `day=*/*.csv` 多 part 文件布局，后续消费脚本需要按目录下所有 part 文件读取。

---

## 9. 当前服务筛选规则与探索性结果

### 9.1 当前默认筛选规则

若仅基于 `service_metadata` 做第一轮粗筛，当前建议规则为：

- `total_active_minutes >= 360`
- `nonzero_in_minutes >= 60`
- `nonzero_ratio = nonzero_in_minutes / total_active_minutes >= 0.1`
- `total_in_calls` 不低于给定分位阈值

当前脚本默认使用：

- `min_active_minutes = 360`
- `min_nonzero_minutes = 60`
- `min_nonzero_ratio = 0.10`
- `min_total_in_quantile = 0.50`

### 9.2 当前质量分数定义

`filter_services_from_metadata.py` 中当前质量分数定义为：

`quality_score = log1p(total_in_calls) + 0.5 * nonzero_ratio + 0.2 * log1p(total_active_minutes) - 0.05 * log1p(burst_ratio)`

其中：

- `burst_ratio = max_in_calls / avg_in_calls`，若 `avg_in_calls = 0`，则退化为 `max_in_calls`

该分数的作用是：

- 优先保留总流量高、活跃度高、非零密度高的服务；
- 对极端突发但整体不稳定的服务做轻微惩罚。

### 9.3 基于前 200 个 `CallGraph` 文件的探索性记录

当前已经对 `CallGraph` 前 `200` 个文件做过一次探索性提取与筛选。需要强调：

- 这是前缀子集分析；
- 可用于规则验证、质量检查和参数预判；
- 不等价于完整 1 天，也不应直接替代全量统计结论。

当前 `top200` 提取得到：

- `total_services = 43909`
- `hard_filtered_services = 9373`
- `selected_services = 4687`

对应阈值解析为：

- `min_active_minutes = 360`
- `min_nonzero_minutes = 60`
- `min_nonzero_ratio = 0.1`
- `min_total_in_quantile = 0.5`
- `resolved_total_in_calls_threshold = 11272`

这组结果说明：

- 约 `21.35%` 的服务通过硬阈值；
- 约 `10.67%` 的服务进入第一轮目标服务集合；
- 当前筛选口径在探索阶段是可工作的。

同时记录以下观察：

- `total_active_minutes` 分布长尾明显；
- `total_in_calls` 分布长尾明显；
- 存在一批在整个观察窗口内几乎始终活跃、且 `nonzero_ratio = 1.0` 的核心服务；
- `UNAVAILABLE` 在当前子集上的质量分数和总入流量都排在前列，它更像系统性占位节点或特殊聚合节点：
  - 不建议把它当作普通业务服务监督目标；
  - 是否保留为上下文节点，需要在图构造阶段单独判断。

---

## 10. 当前局部子图候选构造规则

`build_target_subgraph_candidates.py` 当前使用全量聚合边权构图，再围绕目标服务做局部扩展。

### 10.1 聚合边权

- 读取 `service_call_edge_minute/day=*/*.csv`
- 对所有天、所有 part 文件中的 `(caller_service, callee_service)` 做 `call_count` 累加
- 得到全局有向加权图

### 10.2 当前局部扩展策略

- 先取目标服务的 `1-hop` 上游和下游；
- 再对 `1-hop` 节点做 `2-hop` 外扩；
- 每一步都按边权排序后取前若干邻居；
- 最终用 `max_nodes` 控制子图规模。

当前主要控制参数：

- `max_hop`
- `topk_in`
- `topk_out`
- `topk_next_hop`
- `max_nodes`

### 10.3 当前语义约束

局部子图的作用是提供结构上下文，而不是替代全量真实统计。

因此需要明确区分：

- 来自全量聚合表的真实节点级统计；
- 从局部子图中重新计算得到的局部上下文统计。

后者若使用，建议显式命名为 `local_*`，避免和全量真实值混淆。

---

## 11. 当前已经提取到什么程度

截至目前，`Ali2022` 已经完成的是：

1. 基于 `CallGraph` 的第一版分钟级主干提取；
2. 基于 `service_metadata` 的第一轮目标服务筛选；
3. 基于筛选结果的局部子图候选构造；
4. 对前 `200` 个 `CallGraph` 文件的探索性质量分析与局部子图分析。

这意味着当前已经具备：

- 服务级主标签：`service_in_calls_per_min`
- 边级主标签：`call_count`
- 多变量 baseline 所需的固定维度主干特征；
- 局部子图建模所需的边结构与目标服务候选。

但当前尚未完成的完整数据集部分包括：

- 基于 `MSMetrics` 的服务级 CPU / memory 特征；
- 基于 `CallGraph.rt` 的更完整服务级时延汇总；
- 基于 `MSRTMCR` 的更细粒度交互统计；
- 对更大子集或全量 `CallGraph` 的正式复核与稳定阈值确认。

---

## 12. 当前推荐使用方式

若当前目的是尽快验证统一任务与模型主线，则建议：

1. 先使用当前 `CallGraph` 主干表完成第一版建模；
2. 先做服务级未来入负载预测主任务；
3. 在此基础上对比非图多变量模型与局部子图模型；
4. 等主线跑通后，再逐步补接 `MSMetrics` 和 `MSRTMCR`，扩展成更完整的数据集版本。

---

## 13. 后续扩展提醒

后续若把 `Ali2022` 扩展成完整数据集，需要明确保持以下原则：

- 第一版 `CallGraph` 主干提取规则不能丢；
- 当前筛选规则、质量分数和 `top200` 探索结论需要保留，便于后续对照；
- 后续新增特征应作为“增量扩展”，而不是覆盖或忘掉当前主干版本；
- 文档、脚本和输出目录应始终对应同一套可复现流程。
